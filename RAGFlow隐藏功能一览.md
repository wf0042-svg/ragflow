# RAGFlow 隐藏功能一览

> 本文档记录了 RAGFlow 中所有需要连接外网的功能，用于内网环境部署时的功能隐藏参考。
>
> 创建时间：2026-02-01

---

## 🌐 需要外网连接的功能清单

### 📍 一、Agent 工具（Agent Canvas 中的工具）

#### 🔍 搜索引擎类（7个）

| 序号 | 功能名称 | 外部服务 | API Key 要求 | 后端实现路径 |
|-----|---------|---------|-------------|-------------|
| 1 | **DuckDuckGo 搜索** | DuckDuckGo API | ❌ 不需要 | `agent/tools/duckduckgo.py` |
| 2 | **Google 搜索** | SerpApi | ✅ 需要 | `agent/tools/google.py` |
| 3 | **Tavily 搜索** | Tavily API | ✅ 需要 | `agent/tools/tavily.py` |
| 4 | **Tavily 提取** | Tavily Extract API | ✅ 需要 | `agent/tools/tavily.py` |
| 5 | **Wikipedia** | Wikipedia API | ❌ 不需要 | `agent/tools/wikipedia.py` |
| 6 | **SearXNG** | 自建 SearXNG 实例 | ❌ 不需要 | `agent/tools/searxng.py` |
| 7 | **网页爬虫 (Crawler)** | 目标网站 | ❌ 不需要 | `agent/tools/crawler.py` |

**界面位置**：Agent 页面 → Agent Canvas → 添加工具按钮 → 工具列表

**前端组件标识**：
- `Operator.DuckDuckGo`
- `Operator.Google`
- `Operator.TavilySearch`
- `Operator.TavilyExtract`
- `Operator.Wikipedia`
- `Operator.SearXNG`、
- `Operator.Crawler`

---

#### 📚 学术搜索类（3个）

| 序号 | 功能名称 | 外部服务 | API Key 要求 | 后端实现路径 |
|-----|---------|---------|-------------|-------------|
| 8 | **ArXiv** | ArXiv API | ❌ 不需要 | `agent/tools/arxiv.py` |
| 9 | **PubMed** | PubMed/NCBI API | ❌ 不需要 | `agent/tools/pubmed.py` |
| 10 | **Google Scholar** | Google Scholar | ❌ 不需要 | `agent/tools/googlescholar.py` |

**界面位置**：Agent 页面 → Agent Canvas → 添加工具按钮 → 工具列表

**前端组件标识**：
- `Operator.ArXiv`
- `Operator.PubMed`
- `Operator.GoogleScholar`

---

#### 💻 开发者工具（1个）

| 序号 | 功能名称 | 外部服务 | API Key 要求 | 后端实现路径 |
|-----|---------|---------|-------------|-------------|
| 11 | **GitHub** | GitHub API | ❌ 不需要 | `agent/tools/github.py` |

**界面位置**：Agent 页面 → Agent Canvas → 添加工具按钮 → 工具列表

**前端组件标识**：`Operator.GitHub`

---

#### 💰 金融数据类（5个）

| 序号 | 功能名称 | 外部服务 | API Key 要求 | 后端实现路径 |
|-----|---------|---------|-------------|-------------|
| 12 | **Yahoo Finance** | Yahoo Finance API | ❌ 不需要 | `agent/tools/yahoofinance.py` |
| 13 | **问财 (WenCai)** | 同花顺问财 API | ❌ 不需要 | `agent/tools/wencai.py` |
| 14 | **AkShare** | AkShare 数据源 | ❌ 不需要 | `agent/tools/akshare.py` |
| 15 | **TuShare** | TuShare API | ✅ 需要 token | `agent/tools/tushare.py` |
| 16 | **金十数据 (Jin10)** | 金十数据 API | ✅ 需要 secret_key | `agent/tools/jin10.py` |

**界面位置**：Agent 页面 → Agent Canvas → 添加工具按钮 → 工具列表

**前端组件标识**：
- `Operator.YahooFinance`
- `Operator.WenCai`
- `Operator.AkShare`
- `Operator.TuShare`
- `Operator.Jin10`

---

#### 🌐 其他工具（3个）

| 序号 | 功能名称 | 外部服务 | API Key 要求 | 后端实现路径 |
|-----|---------|---------|-------------|-------------|
| 17 | **Email 发送** | SMTP 服务器 | ⚠️ 需要配置 | `agent/tools/email.py` |
| 18 | **DeepL 翻译** | DeepL API | ✅ 需要 auth_key | `agent/tools/deepl.py` |
| 19 | **和风天气 (QWeather)** | 和风天气 API | ✅ 需要 web_apikey | `agent/tools/qweather.py` |

**界面位置**：Agent 页面 → Agent Canvas → 添加工具按钮 → 工具列表

**前端组件标识**：
- `Operator.Email`
- `Operator.DeepL`
- `Operator.QWeather`

**特殊说明**：
- Email 工具如果公司有内网 SMTP 服务器，可以保留使用

---

### 📁 二、数据源同步功能（用户设置中）

#### 云存储同步（8个）

| 序号 | 数据源名称 | 外部服务 | 认证方式 | 前端路径 | 后端实现路径 |
|-----|-----------|---------|---------|---------|-------------|
| 1 | **Google Drive** | Google Drive API | OAuth 2.0 | `web/src/pages/user-setting/data-source/component/google-drive-token-field.tsx` | `common/data_source/google_drive/connector.py` |
| 2 | **Gmail** | Gmail API | OAuth 2.0 | `web/src/pages/user-setting/data-source/component/gmail-token-field.tsx` | `common/data_source/google_util/` |
| 3 | **Confluence** | Confluence API | OAuth 2.0 | `web/src/pages/user-setting/data-source/constant/confluence-constant.tsx` | `common/data_source/confluence_connector.py` |
| 4 | **Notion** | Notion API | API Token | 常量定义存在 | `common/data_source/notion_connector.py` |
| 5 | **SharePoint** | SharePoint API | 需要配置 | 代码存在 | `common/data_source/sharepoint_connector.py` |
| 6 | **Box** | Box API | OAuth 2.0 | `web/src/pages/user-setting/data-source/component/box-token-field.tsx` | `api/apps/connector_app.py` |
| 7 | **S3 兼容存储** | S3/MinIO/OSS 等 | Access Key | `web/src/pages/user-setting/data-source/constant/s3-constant.tsx` | `common/data_source/blob_connector.py` |
| 8 | **Bitbucket** | Bitbucket API | 需要配置 | `web/src/pages/user-setting/data-source/constant/bitbucket-constant.tsx` | 代码中有定义 |

**界面位置**：右上角用户头像 → 用户设置 (Settings) → 数据源 (Data Source) → 添加数据源

**后端 OAuth API 路径**：
- Google Drive OAuth: `/v1/connector/google-drive/oauth/web/start`
- Gmail OAuth: `/v1/connector/gmail/oauth/web/start`
- Box OAuth: `/v1/connector/box/oauth/web/start`
- OAuth 实现文件: `api/apps/connector_app.py`

**特殊说明**：
- S3 兼容存储如果公司有内网 MinIO/OSS，可以保留使用

---

### 🤖 三、LLM 相关（如果使用云端服务）

| 功能类型 | 说明 | 配置位置 |
|---------|------|---------|
| **云端 LLM 模型** | OpenAI、Claude、Gemini、通义千问等 | 用户设置 → 模型提供商 |
| **云端 Embedding 模型** | OpenAI Embeddings、通义千问 Embeddings 等 | 用户设置 → 模型提供商 |

**特殊说明**：
- 如果使用公司内部部署的 Qwen 模型，不需要隐藏此功能
- 只需要在配置中指向内网 LLM 服务地址即可

---

## ✅ 建议隐藏的功能分类

### 🔴 高优先级（强烈建议隐藏）

#### Agent 工具类（11个）
- ✅ DuckDuckGo 搜索
- ✅ Google 搜索
- ✅ Tavily 搜索
- ✅ Tavily 提取
- ✅ Wikipedia
- ✅ SearXNG
- ✅ 网页爬虫 (Crawler)
- ✅ ArXiv
- ✅ PubMed
- ✅ Google Scholar
- ✅ GitHub

**理由**：这些工具都需要访问外部互联网，在内网环境中无法使用。

#### 数据源同步类（7个）
- ✅ Google Drive
- ✅ Gmail
- ✅ Confluence
- ✅ Notion
- ✅ SharePoint
- ✅ Box
- ✅ Bitbucket

**理由**：这些都是云端服务，需要通过互联网访问。

---

### 🟡 中优先级（可选隐藏）

#### 金融数据工具（5个）
- Yahoo Finance
- 问财 (WenCai)
- AkShare
- TuShare
- 金十数据 (Jin10)

**建议**：
- 如果公司业务不涉及金融数据分析，建议隐藏
- 如果需要金融数据功能，可以保留

#### 其他工具（2个）
- DeepL 翻译
- 和风天气 (QWeather)

**建议**：
- 这些是辅助功能，根据实际需求决定
- 如果不需要，建议隐藏以简化界面

---

### 🟢 低优先级（可保留）

#### 可能在内网使用的功能
- **Email 发送工具**
  - 如果公司有内网 SMTP 服务器，可以保留
  - 如果没有或不需要，建议隐藏

- **S3 兼容存储**
  - 如果公司有内网（MinIO/OSS），可以保留
  - 如果只用本地文件上传，可以隐藏

- **本地 LLM 配置**
  - 使用公司内部 Qwen 模型，保留此功能
  - 只需配置指向内网服务地址

---

## 🎯 推荐方案

### 方案一：最小化方案（最安全）

**隐藏内容**：
- 所有 19 个 Agent 工具
- 所有 8 个数据源同步功能

**保留内容**：
- 本地文件上传
- 公司内部 Qwen 模型配置
- 基础的知识库和对话功能

**适用场景**：
- 对安全要求极高的环境
- 不需要任何外部数据源的场景

---

### 方案二：实用化方案（推荐）⭐

**隐藏内容**：
- 所有搜索引擎和学术搜索工具（10个）
- 所有云存储同步功能（8个）
- 金融数据工具（根据需求）
- 翻译和天气工具（根据需求）

**保留内容**：
- Email 工具（如果有内网 SMTP）
- S3 存储（如果有内网对象存储）
- 本地文件上传
- 公司内部 Qwen 模型

**适用场景**：
- 大多数企业内网环境
- 需要基本的文档处理和对话功能
- 有内网基础设施支持

---

### 方案三：定制化方案

根据公司实际需求，选择性隐藏功能。

**需要确认的问题**：
1. 是否需要金融数据分析功能？
2. 是否有内网 SMTP 服务器？
3. 是否有内网对象存储（MinIO/OSS）？
4. 是否需要翻译功能？
5. 是否需要天气查询功能？

---

## 📝 实施说明

### 前端隐藏方式

1. **Agent 工具隐藏**
   - 修改前端组件，过滤掉需要隐藏的工具
   - 相关文件：`web/src/pages/flow/` 目录下的组件

2. **数据源隐藏**
   - 修改用户设置页面，移除相关数据源选项
   - 相关文件：`web/src/pages/user-setting/data-source/`

### 后端禁用方式

1. **工具注册禁用**
   - 在工具注册时跳过需要隐藏的工具
   - 相关文件：`agent/component/` 和 `agent/tools/`

2. **API 端点禁用**
   - 禁用相关的 OAuth 和数据源 API
   - 相关文件：`api/apps/connector_app.py`

### 配置文件方式

建议通过配置文件控制功能开关，便于不同环境的部署：

```yaml
# config/features.yaml
features:
  agent_tools:
    search_engines: false      # 搜索引擎工具
    academic_search: f学术搜索工具
    developer_tools: false     # 开发者工具
    financial_data: false      # 金融数据工具
    translation: false         # 翻译工具
    weather: false            # 天气工具
    email: true               # Email（如果有内网 SMTP）

  data_sources:
    cloud_storage: false      # 云存储同步
    s3_compatible: true       # S3 兼容存储（如果有内网）
```

---

## 📊 统计信息

- **Agent 工具总数**：19 个
  - 搜索引擎类：7 个
  - 学术搜索类：3 个
  - 开发者工具：1 个
  - 金融数据类：5 个
  - 其他工具：3 个

- **数据源总数**：8 个
  - 云存储服务：8 个

- **建议隐藏总数**：
  - 高优先级：18 个（11 个工具 + 7 个数据源）
  - 中优先级：7 个（5 个金融工具 + 2 个其他工具）
  - 可选保留：2 个（Email + S3）

---

## 🔗 相关文档

- [CLAUDE_cn.md](./CLAUDE_cn.md) - RAGFlow 开发指南
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - 故障排查文档
- [README.md](./README.md) - 项目说明

---

## 📅 更新记录

| 日期 | 版本 | 更新内容 | 更新人 |
|-----|------|---------|--------|
| 2026-02-01 | v1.0 | 初始版本，完整功能清单 | 王健 |

---

## 💡 注意事项

1. **隐藏功能不等于删除代码**
   - 建议通过配置开关控制功能显示
   - 保留代码便于将来需要时快速恢复

2. **测试验证**
   - 隐藏功能后需要全面测试
   - 确保不影响其他功能的正常使用

3. **文档同步**
   - 更新用户手册，说明哪些功能不可用
   - 培训用户了解内网版本的功能限制

4. **版本管理**
   - 建议创建内网专用分支
   - 与官方版本保持同步更新

---

**文档维护者**：王健
**最后更新**：2026-02-01
